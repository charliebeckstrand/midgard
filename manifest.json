{
	"version": 1,
	"generatedAt": "2026-05-15T18:27:45Z",
	"packageManager": "pnpm",
	"monorepo": {
		"tool": "turbo",
		"workspaces": [
			"apps/*",
			"packages/*"
		]
	},
	"packages": [
		{
			"name": "admin",
			"path": "apps/admin",
			"framework": "next",
			"testRunner": null,
			"linter": "biome",
			"scripts": {
				"test": null,
				"test:changed": null,
				"test:related": null,
				"lint": "biome check --config-path=../.. .",
				"check-types": "tsc --noEmit",
				"build": "next build",
				"dev": "next dev --port 3000 --turbopack"
			},
			"componentsDir": null,
			"primitivesDir": null,
			"hooksDir": null,
			"tokensDir": null,
			"testLayout": null,
			"testHelpersDir": null,
			"isFrontend": true
		},
		{
			"name": "chat",
			"path": "apps/chat",
			"framework": "next",
			"testRunner": null,
			"linter": "biome",
			"scripts": {
				"test": null,
				"test:changed": null,
				"test:related": null,
				"lint": "biome check --config-path=../.. .",
				"check-types": "tsc --noEmit",
				"build": "next build",
				"dev": "next dev --port 3001 --turbopack"
			},
			"componentsDir": null,
			"primitivesDir": null,
			"hooksDir": null,
			"tokensDir": null,
			"testLayout": null,
			"testHelpersDir": null,
			"isFrontend": true
		},
		{
			"name": "auth",
			"path": "packages/auth",
			"framework": "next",
			"testRunner": null,
			"linter": "biome",
			"scripts": {
				"test": null,
				"test:changed": null,
				"test:related": null,
				"lint": "biome check --config-path=../.. .",
				"check-types": null,
				"build": "tsup",
				"dev": "tsup --watch"
			},
			"componentsDir": null,
			"primitivesDir": null,
			"hooksDir": null,
			"tokensDir": null,
			"testLayout": null,
			"testHelpersDir": null,
			"isFrontend": false
		},
		{
			"name": "shared",
			"path": "packages/shared",
			"framework": "next",
			"testRunner": null,
			"linter": "biome",
			"scripts": {
				"test": null,
				"test:changed": null,
				"test:related": null,
				"lint": "biome check --config-path=../.. .",
				"check-types": null,
				"build": "tsup",
				"dev": "tsup --watch"
			},
			"componentsDir": null,
			"primitivesDir": null,
			"hooksDir": null,
			"tokensDir": null,
			"testLayout": null,
			"testHelpersDir": null,
			"isFrontend": true
		},
		{
			"name": "ui",
			"path": "packages/ui",
			"framework": "react",
			"testRunner": "vitest",
			"linter": "biome",
			"scripts": {
				"test": "vitest run",
				"test:changed": "vitest run --changed",
				"test:related": "vitest related --run",
				"lint": "biome check --config-path=../.. .",
				"check-types": "tsc --noEmit && tsc --noEmit -p tsconfig.scripts.json",
				"build": "tsup && tsc --emitDeclarationOnly --outDir dist",
				"dev": "trap 'kill 0' EXIT; tsup --watch --no-dts & vite --config vite.docs.config.ts & wait"
			},
			"componentsDir": "src/components",
			"primitivesDir": "src/primitives",
			"hooksDir": "src/hooks",
			"tokensDir": "src/recipes",
			"testLayout": "mirror",
			"testHelpersDir": "src/__tests__",
			"isFrontend": true
		}
	],
	"preCommit": {
		"tool": "lefthook",
		"gates": [
			"lint",
			"type",
			"filenames",
			"test"
		]
	},
	"ci": {
		"provider": null,
		"jobs": []
	},
	"conventions": {
		"files": [
			"CLAUDE.md"
		],
		"principles": [
			"Simplicity above all; abstractions are extracted from proven duplication, never predicted.",
			"Solve the stated problem, not adjacent ones — a bug fix is not a refactoring opportunity.",
			"Dependencies flow inward: shared packages never depend on application code.",
			"Let the type system carry its weight; if a type is hard to express, rethink the design.",
			"packages/ui/src/core/recipe/ holds the recipe engine (defineRecipe, palette, merge). packages/ui/src/recipes/ has the design layer: kiso substrate (iro · ji · ma · narabi · omote · hannou · sen · shaku · tsunagi · ugoki · kokkaku), kata per-component recipes, and genkei multi-element archetypes; the recipes barrel re-exports the engine so consumers get one import surface. The internal boundary is pinned by src/__tests__/recipes/boundary/recipe-boundary.test.ts.",
			"Files must be self-identifying out of context: components use <name>.tsx / <name>-<part>.tsx; hooks use use-<name>-<hook>.ts; every component or hook file exports a symbol whose PascalCase form matches its filename (enforced by packages/ui/src/__tests__/components/boundary/component-filename-boundary.test.ts).",
			"Never extend the inline ALLOWLIST in component-filename-boundary.test.ts for new files — fix the file or fix the export instead.",
			"Imperative-mood, atomic commits; each commit represents one logical change.",
			"Inside the editing loop, never run the full ui test suite (`pnpm --filter ui test` or `pnpm test`); use scoped commands and let the lefthook pre-commit gate catch the rest.",
			"When a check fails after an edit, fix the cause; never weaken the check (no deleting assertions, .skip, or stripping expect calls to make a suite green).",
			"Work is done only when /postmortem returns PROCEED and the diff has been self-reviewed."
		],
		"vocabularyGlossary": {
			"engine": "The recipe DSL under packages/ui/src/core/recipe/engine/ — `defineRecipe(config)` plus `palette(matrix, ...overlays)`. Reserved fields `base`, `palette`, `compound`, `slots`, `defaults`; any other top-level field is a variant axis.",
			"kiso": "Design-token substrate (基礎 — foundation) under packages/ui/src/recipes/kiso/ — iro · ji · ma · narabi · omote · hannou · sen · shaku · tsunagi · ugoki · kokkaku. Re-exported from packages/ui/src/recipes/index.ts for in-package consumers.",
			"kata": "Internal per-component recipe layer (型 — form) under packages/ui/src/recipes/kata/ — every file exports the primary recipe as `k`. Imported via relative paths, deliberately not in package.json exports.",
			"genkei": "Internal multi-element archetype layer (原型 — archetype) under packages/ui/src/recipes/genkei/ — shared structural templates (panel, control, option, popover, kasane) instantiated by ≥2 kata. Same internal-only boundary as kata.",
			"Manifest": "Canonical project profile at ./manifest.json produced by /repo:manifest and consumed by every other skill.",
			"Brief": "Output of /deliberate — a precise problem statement synthesized from ABCD-style answers to dimension gaps."
		},
		"mandatorySkills": [
			{
				"skill": "/repo:manifest",
				"trigger": "./manifest.json is missing or invalidated (every other skill stops with this instruction when absent)"
			},
			{
				"skill": "/deliberate",
				"trigger": "request is a fuzzy idea from which no concrete proposal can be formed"
			},
			{
				"skill": "/postmortem",
				"trigger": "before declaring work done — must return PROCEED, or every chained downstream skill must return PASS"
			},
			{
				"skill": "/ui:component:compose",
				"trigger": "scaffolding a new UI component in packages/ui — canonical source for [layout-heuristics] and [framework-discipline]"
			},
			{
				"skill": "/tests:compose",
				"trigger": "writing tests for components, hooks, utilities, or modules"
			},
			{
				"skill": "/typescript:format",
				"trigger": "after any session that touched .ts / .tsx — invoked first in /postmortem's chain and listed in CLAUDE.md § Quality check as the per-session format gate"
			},
			{
				"skill": "/typescript:review",
				"trigger": "only via /postmortem (diff mode) or /ui:component:compose & /tests:compose (file mode) — do not invoke directly outside those paths unless explicitly asked"
			}
		]
	},
	"notes": [
		"apps/tms, packages/heimdall, and packages/sindri exist on disk as residual directories without package.json (only .turbo/ and dist/ artifacts). Git log shows tms was removed and heimdall/sindri were renamed to auth/shared — these directories should be cleaned up.",
		"Both .husky/ and lefthook.yml are present; lefthook is the active tool (root package.json `prepare` script runs `lefthook install`).",
		"No CI provider detected — no .github/workflows, .gitlab-ci.yml, or .circleci/config.yml.",
		"packages/ui is the only package with a test runner (vitest), matching the documented testing policy in CLAUDE.md.",
		"packages/auth and packages/shared list `next` in devDependencies/peerDependencies and so resolve to framework=next per the discovery table, though they ship as libraries (tsup build) consumed by the Next apps."
	]
}