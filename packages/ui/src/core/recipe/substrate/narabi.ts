/**
 * Narabi (並び) — sibling / slot layout (panel slots, list items, toggle /
 * group, field).
 *
 * Re-exported from `recipes/ryu/narabi` pending a substrate-side port.
 *
 * Layer: ryū · Concern: layout
 */

export { narabi } from '../../../recipes/ryu/narabi'
