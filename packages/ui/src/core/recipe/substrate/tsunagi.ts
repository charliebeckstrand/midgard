/**
 * Tsunagi (繋ぎ) — group joins.
 *
 * Class fragments for items in a group (button group, toolbar, segmented
 * control). Position is stamped at runtime by the parent `<Group>` wrapper as
 * `data-group` / `data-group-orientation` attributes.
 *
 * Re-exported from `recipes/ryu/tsunagi` pending a substrate-side port.
 *
 * Layer: ryū · Concern: group joins
 */

export { type GroupOrientation, type GroupPosition, tsunagi } from '../../../recipes/ryu/tsunagi'
