/**
 * Omote (表) — surface chromes (panel / popover / glass / backdrop bundles).
 *
 * Re-exported from `recipes/ryu/omote` pending a substrate-side port.
 *
 * Layer: ryū · Concern: surfaces
 */

export { omote } from '../../../recipes/ryu/omote'
