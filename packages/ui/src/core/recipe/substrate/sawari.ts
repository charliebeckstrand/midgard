/**
 * Sawari (触り) — interaction states (hover / press / disabled / cursor).
 *
 * Re-exported from `recipes/ryu/sawari` pending a substrate-side port.
 *
 * Layer: ryū · Concern: interaction
 */

export { sawari } from '../../../recipes/ryu/sawari'
