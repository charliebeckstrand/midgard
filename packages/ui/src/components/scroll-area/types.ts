/**
 * Scrollbar visibility behavior: `auto` fades in while scrolling, `visible`
 * stays shown, `hidden` removes the overlay scrollbars.
 *
 * @internal
 */
export type ScrollbarMode = 'auto' | 'visible' | 'hidden'
