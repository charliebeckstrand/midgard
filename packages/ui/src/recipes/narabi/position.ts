const start = 'flex items-start justify-start'
const center = 'flex items-center justify-center'
const centerInline = 'inline-flex items-center justify-center'
const end = 'flex items-end justify-end'
const isolate = 'relative isolate'

export const position = {
	isolate,
	start,
	center,
	centerInline,
	end,
} as const
