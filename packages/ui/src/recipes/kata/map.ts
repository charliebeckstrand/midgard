import { hannou, sen } from '../kiso'
export const k = {
	base: [
		'relative isolate overflow-hidden',
		'w-full h-full',
		sen.border,
		'rounded-lg',
		hannou.disabled,
	],
	canvas: ['absolute inset-0'],
}
