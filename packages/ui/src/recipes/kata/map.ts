import { sawari, sen } from '../../core/recipe'

export const k = {
	base: [
		'relative isolate overflow-hidden',
		'w-full h-full',
		sen.border,
		'rounded-lg',
		sawari.disabled,
	],
	canvas: ['absolute inset-0'],
}
