/**
 * Ma (間) — Space.
 *
 * The emptiness that gives form meaning — padding, gaps, breathing room.
 * Ma is not absence; it is the deliberate placement of nothing.
 *
 * Branch of: Ma (root)
 * Concern: spacing
 */
export const ma = {
	/** The internal breathing room of a form control */
	control:
		'px-[calc(--spacing(3.5)-1px)] py-[calc(--spacing(2.5)-1px)] sm:px-[calc(--spacing(3)-1px)] sm:py-[calc(--spacing(1.5)-1px)]',

	/** The inset between a floating panel and the viewport edge */
	float: 'p-4',
}
