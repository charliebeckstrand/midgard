import { soft, softHover, solid, solidHover, withHover } from './palette'

export const badgeSoft = withHover(soft, softHover, 'group-hover:')

export const badgeSolid = withHover(solid, solidHover, 'group-hover:')
