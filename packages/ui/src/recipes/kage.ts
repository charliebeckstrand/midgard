/**
 * Kage (影) — Shadow.
 *
 * Box-shadow tokens — the depth cue for floating surfaces.
 * Lines (borders, rings, dividers) live in `sen`.
 *
 * Tier: 1 · Concern: shadow
 */

export const kage = {
	xs: 'shadow-xs',
	sm: 'shadow-sm',
	md: 'shadow-md',
	lg: 'shadow-lg',
} as const
