/**
 * Popover archetype — trigger. The anchor element class. Keeps the
 * trigger inline-aligned with surrounding text or icons.
 *
 * Layer: kiso · Archetype: popover · Concern: trigger
 */

export const trigger = 'inline-flex'
