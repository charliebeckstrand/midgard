/**
 * Kokkaku skeleton — radio. Fixed 4.5-square circle silhouette.
 *
 * Layer: kiso · Concern: skeleton form · Unit: radio
 */

import { kasane } from '../kasane'

export const radio = {
	base: ['size-4.5', kasane.rounded.full],
} as const
