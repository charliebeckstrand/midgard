/**
 * Omote surface — solid surface background. The plane cards, sidebars,
 * and navbars sit on.
 *
 * Layer: kiso · Concern: surface fill
 */

import { bg } from './bg'

export const surface = bg.surface
