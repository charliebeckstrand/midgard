/**
 * Omote tint — subtle tinted overlay. Used for raised / striped rows
 * where a full surface fill would over-emphasise.
 *
 * Layer: kiso · Concern: tinted overlay
 */

import { bg } from './bg'

export const tint = bg.tint
