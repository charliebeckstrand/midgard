/**
 * Text color recipes — semantic ink colors for light/dark mode.
 *
 * Single concern: color. Each encodes a light/dark pair.
 * Changing the design system's text palette means editing these two values.
 */

/** Ink: the default writing color — high-contrast text for headings, labels, body */
export const ink = 'text-zinc-950 dark:text-white'

/** Muted: supporting text — descriptions, placeholders, secondary labels */
export const muted = 'text-zinc-500 dark:text-zinc-400'
