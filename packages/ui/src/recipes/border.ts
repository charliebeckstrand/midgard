/**
 * Border color recipes — the semantic border pairs used across the system.
 *
 * Each recipe is a single concern (color) encoding a light/dark pair.
 */

/** Default border: the standard subtle separator */
export const borderDefault = 'border-zinc-950/10 dark:border-white/10'

/** Soft border: even more subtle (dividers within content) */
export const borderSoft = 'border-zinc-950/5 dark:border-white/5'
