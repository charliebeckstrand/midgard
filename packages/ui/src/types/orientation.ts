/** Layout / navigation axis: the direction a component lays out or arrow-key navigates along. */
export type Orientation = 'horizontal' | 'vertical'

/** Scroll axis; widens {@link Orientation} with `'both'` for surfaces that pan on two axes. */
export type ScrollOrientation = 'horizontal' | 'vertical' | 'both'
