// Root entry is reserved for app-level providers — things consumers wrap their
// entire tree with from a root layout. All other exports live on dedicated
// subpaths (`ui/<component>`, `ui/hooks`, `ui/layouts`, `ui/types`).
export { LinkProvider } from './primitives'
