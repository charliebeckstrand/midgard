# Documentation Audit — repo-wide

**Date:** 2026-06-22 · **Scope:** the **whole monorepo, comments included** — `packages/ui`, `packages/auth`, `packages/shared`, `apps/admin`, and the markdown surface docs. This widens the lens beyond the prior two passes ([2026-06-13](2026-06-13-DOC-AUDIT.md) surface, [2026-06-14](2026-06-14-DOC-AUDIT.md) ui comments-included): it re-verifies the `ui` surface against the ~180 source files that changed since 06-14, and runs the **first-ever** sweep over the non-`ui` workspaces (`auth`, `shared`, `admin`), which no prior audit covered. **Method:** the TSDoc standard (tags / syntax) was pulled from the documentation MCP knowledge base; the sweep was delegated one subsystem per subagent ([CLAUDE.md](../../../../CLAUDE.md) §3.3). **All edits are comment-only — no code, types, imports, JSX, or formatting changed**; a diff scan confirms zero non-comment changed lines. Verified: `biome check .` (1,613 files, no fixes) and `turbo run check-types` (tsc across all 4 packages incl. ui's scripts/docs configs) green.

This is a living record per [`CONVENTIONS.md`](../../../../CONVENTIONS.md) §12.3, accompanying the curated surface docs in [`../`](..).

---

## Executive summary

Since the 06-14 deep sweep the library churned heavily — the `Headless`→`HeadlessProvider` move under `providers/`, `Message`/`Text` `variant`→`severity`, severity-driven validation on Field/Control, DataTable column drag-reordering and the column-manager dialog, clearable DateInput/DatePicker, TagInput read-only-at-cap, NumberInput bare-variant buttons. The headline finding is that the public surface held: the 06-14 bar survived the refactors, and the renames were already reconciled in code comments and in the curated surface indices. The yield concentrated in two places — recipe-layer module headers that drifted as feature slots were added, and the never-audited non-`ui` workspaces, where the public surface simply had no TSDoc yet.

1. **`ui` public surface re-verified, no regressions.** The five named renames are clean everywhere: no comment still calls `Headless` a component, `severity` is consistently described, and `COMPONENTS.md` / `PROVIDERS.md` already match the barrels. Targeted gaps were filled (`SidebarLayout*`, `ControlPropsOptions`/`ControlPropsResult`, the two Kanban context hooks) and `@internal` summaries added on non-exported helpers across json-tree, stepper, tree, menu, recipe engine, typeahead.

2. **`auth` / `shared` / `admin` documented for the first time.** 22 files gained summary-first TSDoc on the public surface — `bifrost`, `getUser`/`User`, `withAuth`, `proxy`/`ProxyOptions`, `BIFROST_URL`; the chat hooks (`useChat`, `useSendMessage`) and types; the admin server fetchers and dialogs — plus `@internal` on the validator factories and form internals.

3. **Stale recipe headers and comments corrected.** The highest-value finds of a "comments included" sweep: kata module headers that no longer listed feature slots added by the refactor, and a kiso README file table that fell behind the wide-palette work.

4. **Standard normalization, continued.** Residual `@default`→`@defaultValue`, prose "Defaults to X" → `@defaultValue`, and bare symbol refs → `{@link}` in the changed files.

## Stale or inaccurate comments fixed

These are the comment-vs-code contradictions the sweep caught — recorded because they are the part most likely to mislead:

- **`components/table/table-empty.tsx`** — `@defaultValue` claimed the empty default was the string `'No items'`; it is actually `<TableEmptyAlert />`, a soft `Alert` titled "No items". Corrected.
- **`components/tabs/tab-list-skeleton.tsx`** — said placeholders sit "above" the rail; they render as flex children inside the `k.list` row. Reworded.
- **`components/currency-input/currency-input.tsx`** — the `@remarks` editing-buffer claim had drifted from the current buffering behavior. Corrected.
- **`recipes/kata/tabs.ts`** — header sub-recipe list omitted the exported `scroll` overflow recipe. Added.
- **`recipes/kata/table.ts`** — `projection` doccomment referred to "the **old** tbody `striped` classes"; the `striped` projection key is current. Reworded to "below".
- **`recipes/kata/date-picker.ts`** — header slot list omitted the top-level `surface` and `control` slots (input-mode field adjacency). Added.
- **`recipes/kata/data-table.ts`** — chrome feature list omitted the `reorder` slot group (column drag-reorder). Added.
- **`recipes/kiso/README.md`** §3 — the `iro` row's Files column missed `ramp`, `project`, `spectrum` (added in the wide-palette work); Concern text now names the opt-in `spectrum` palette. Reconciled.
- **`docs/demos/providers/skeleton.tsx`** — a dangling `// (§3.6)` cross-ref pointed at no existing anchor; replaced with the actual API the code calls (`useComboboxQuery`).

## What changed, by area

| Area | This pass |
|---|---|
| `ui` components (a–k) | color/json-tree/kanban `@internal` + the two Kanban context hooks' summaries; currency-input `@remarks` fix; date-picker period-state comment aligned; control `ControlPropsOptions`/`ControlPropsResult` summaries. DataTable reorder/column-manager verified already documented. |
| `ui` components (l–z) | table-empty + tab-list-skeleton stale fixes; `@internal`/normalization across listbox, menu, stepper, tree. |
| `ui` hooks / primitives / providers / core / layouts | `Headless`→`HeadlessProvider` rename verified clean; `@internal` on `use-typeahead`/recipe-engine helpers; `SidebarLayout*` family documented. |
| `ui` recipes + docs-site | kata header slot reconciliations (tabs/table/date-picker/data-table); kiso README `iro` row; `SidebarItemVariants` `{@link}`; demo cross-ref fix. `*Variants` barrel coverage re-verified. |
| `ui` markdown surface indices | `COMPONENTS`/`HOOKS`/`PRIMITIVES`/`PROVIDERS`/`CORE`/`UTILITIES`/`LAYOUTS`/`RECIPES` diffed against barrels — all current; `packages/ui/README.md` §4 stale `REFERENCE.md` description + missing `docs/` pointer fixed. |
| `auth` / `shared` / `admin` (first pass) | 22 files: summary-first TSDoc on the public surface (auth gateway/proxy/env, chat hooks/types, admin server fetchers) + `@internal` on internal helpers. READMEs verified accurate. |

## Coverage

51 files edited (29 `ui`, 10 `shared`, 7 `admin`, 5 `auth`), comment-only. Net **+62** `/**` doccomment blocks (67 added, 5 normalized away). The deletions in the diff are normalizations (non-standard tags rewritten, stale prose replaced), not removed documentation.

## Surfaced issues (code-level)

Per [`CLAUDE.md`](../../../../CLAUDE.md) §1.2, these were flagged not fixed during the comment-only sweep, then addressed in a follow-up code commit:

- **`shared/chat/use-send-message.ts` — RESOLVED.** Failure recovery now keys the placeholder drop off the send's own `agentId` (`filter(m => !(m.id === agentId && m.content === ''))`) instead of matching any empty agent message, so concurrent or prior empty bubbles are untouched; the empty-only check is preserved, so a partial reply survives a mid-stream error.
- **`shared/chat/use-send-message.ts` — RESOLVED (documented).** The `content` SSE events replace (not append to) the bubble; the gateway streams cumulative snapshots. Made explicit with a comment at the SSE boundary. Behavior unchanged — switching to deltas would be a gateway-contract change, out of scope.
- **`recipes/kiso/kokkaku/control.ts` — RESOLVED.** The `<ControlFrame>` consumer list now includes `ColorPicker` (verified: `color-picker-trigger.tsx`, alongside input-frame, textarea, date-picker-trigger, and select-trigger which fronts Select / Listbox / Combobox).
- **`docs/demos/data-table.tsx` — RESOLVED.** The commented-out dead `BatchActionsExample` alternative body was deleted.
- **`components/input/input.tsx` — NOT A DEFECT.** `resolvedVariant` resolving to `'glass'` while `InputProps.variant` is `'default' | 'outline'` is by design: `glass` is ambient styling supplied through `GlassProvider` context, never the public prop ([CONVENTIONS.md](../../../../CONVENTIONS.md) §3.8). No change.
- **`pdf-viewer` "Fit to page" — NOT A DEFECT (06-14 finding withdrawn).** Layout scale is `fitScale * zoom` (`use-pdf-viewer-page-scale.ts`), so resetting `zoom` to `1` *is* fit-to-viewport, not 100% actual size. The label is correct; the [06-14](2026-06-14-DOC-AUDIT.md) note misread the scale model. No change.
- **`date-picker/date-picker-range-reducer.ts` byte-identical `pinEndpoint`/`hover` cases — INTENTIONAL.** The commit-time freeze legitimately mirrors the hover write; documented in place at 06-14. No change.

## Deferred (out of scope this pass)

- **Layouts-family doc gaps — RESOLVED.** `SidebarLayoutFooter` and `StackedLayoutFooter` gained summary-first slot doccomments; the rest of the `*Layout*` families already carry them.
- **`map/map-shipment.tsx`** — the churn note expected a `useActionState` refactor, but the code uses `useState`; no comment asserts an action-state flow, so nothing was stale. Either the refactor never landed or the note was mistaken — a code question, not a doc fix.
- **Uniform kata per-file headers** and **per-token kiso TSDoc** — convention-change scope, deferred by the 06-14 audit; unchanged.
- **Tests & benchmarks** — governed by the test conventions ([CONVENTIONS.md](../../../../CONVENTIONS.md) §10), not this sweep.

## Resolution

Living record. As deferred items are addressed, mark the row resolved in place with the resolving commit. A future full re-sweep takes a new `{date}-DOC-AUDIT.md`.

---

**See also:** [`../../REFERENCE.md`](../../REFERENCE.md) · the curated surface docs in [`../`](..) · prior audits ([DOC 06-14](2026-06-14-DOC-AUDIT.md), [DOC 06-13](2026-06-13-DOC-AUDIT.md), [RECIPES 06-20](2026-06-20-RECIPES-AUDIT.md), [PROP](2026-06-13-PROP-AUDIT.md), [BUG](2026-06-10-BUG-AUDIT.md), [ARIA](2026-06-08-ARIA-AUDIT.md)).
