# Bug Audit — defects across the `ui` surface

**Date:** 2026-09-11 · **Scope:** every `.ts` and `.tsx` file under [`src`](../../src), except every `__tests__` and `__benchmarks__` directory — 1,510 files and 153,159 lines. **Method:** each segment runs research, then resolution, and the ledger tracks them apart. Research sweeps every line of a disjoint file set, verifies each claim adversarially, and ends at a review gate. Resolution closes the findings it raised. **Plan:** [`2026-09-11-BUG-SWEEP-PLAN.md`](../plans/2026-09-11-BUG-SWEEP-PLAN.md) holds the segment design, the ledger, and the coverage. **Living record — resolve rows in place, against the pull request that closed them.**

## Executive summary

Segment `A01` (`3298641`) holds 15 findings over `date-picker`, `menu`, `segment`, and part of `pdf-viewer`: 2 high, 4 medium, and 9 low. Three root causes produce eight of them, so eight rows resolve as three decisions. The two high rows are value defects on documented contracts, and both sit in `DatePicker`.

No claim fell. The first verification pass kept all 15 and refuted none, and a red team then overturned none. That rate does not match this repository's record, so [Method correction](#method-correction) treats it as a fact about the method: both verifiers attacked the mechanism of each claim, and neither attacked its reach. The red team restated two claims and downgraded four on reach grounds alone.

`A01` research is done and its resolution is open. [Recommended resolution](#recommended-resolution--a01) names five steps that close the 15 findings, and the review settled the three questions that gated them.

Segment `A02` (`674adc3`) holds 21 findings over `calendar`, `json-tree`, `kanban`, `list`, `pivot-table`, `stat`, and `tree`: 1 medium-high, 7 medium, and 13 low. No row is high. Nine root causes produce 15 of them, so 15 rows resolve as nine decisions. One row alone reaches a shipped surface: the kanban column's drop-target highlight adds no class.

Six claims fell in `A02`, and two more triggers fell inside rows that survive on another trigger. The method also changed. One blind, batched verifier judged the union of two independent readings. A second, independent judge then re-judged the same stripped claims, and the two agree on survive-or-die for 34 of 34 claims.

`A02` research waits on the reader, and its resolution has not started. [Recommended resolution — `A02`](#recommended-resolution--a02) names seven steps that close the 21 findings, and [eight open questions](#open-questions--a02) gate them. The [plan](../plans/2026-09-11-BUG-SWEEP-PLAN.md) holds `A02` in review until the reader answers them, and it carries the state of the 32 segments that have not started research.

## Findings

Segment `A01` raised 15 claims and all 15 survive verification. Every step is fixed on the branch, and the rows close when a pull request merges. The table is the ledger: it carries the severity, the location, and the resolution state of each finding. `Status` tracks resolution only — every row below is researched. A fix lands as `◐ FIXED` and becomes `✅ RESOLVED` when its pull request merges; `✖ REFUTED` marks a row that resolution disproved, because §12.4 resolves a row against a pull request and a branch commit dangles after a squash merge. No row of this segment carries that marker. The sections below carry the mechanism only, and [Recommended resolution](#recommended-resolution--a01) carries the work.

Three root causes produce eight of the fifteen findings. Each root cause resolves as one decision, so the groups come first.

| # | Severity | Location | Defect | Status |
|---|---|---|---|---|
| R1.1 | high | [`date-picker.tsx:325`](../../src/components/date-picker/date-picker.tsx) | `readOnly` never reaches the `DateInput` | ◐ FIXED |
| R1.2 | medium | [`date-picker.tsx:316`](../../src/components/date-picker/date-picker.tsx) | the resolved `invalid` flag never reaches the `DateInput` | ◐ FIXED |
| R2.1 | high | [`date-picker-relative-utilities.ts:267`](../../src/components/date-picker/date-picker-relative-utilities.ts) | multi-select dedupes preset ids, not spans | ◐ FIXED |
| R2.2 | medium | [`use-date-picker-relative-state.ts:136`](../../src/components/date-picker/use-date-picker-relative-state.ts) | the reference clock sits in a ref that no memo depends on | ◐ FIXED |
| R2.3 | low | [`use-date-picker-relative-state.ts:153`](../../src/components/date-picker/use-date-picker-relative-state.ts) | the custom-range seed reads `value[0]`, not the unmatched span | ◐ FIXED |
| R3.1 | medium | [`menu-content.tsx:73`](../../src/components/menu/menu-content.tsx) | a static menu seats no tab stop | ◐ FIXED |
| R3.2 | low | [`use-menu-state.ts:158`](../../src/components/menu/use-menu-state.ts) | a static menu trigger toggles state the panel ignores | ◐ FIXED |
| R3.3 | low | [`menu-content.tsx:64`](../../src/components/menu/menu-content.tsx) | a static panel omits `id`, so `aria-controls` dangles | ◐ FIXED |
| F1 | low | [`use-date-picker-relative-state.ts:212`](../../src/components/date-picker/use-date-picker-relative-state.ts) | the footer Clear lands focus on the panel, not on a row | ◐ FIXED |
| F2 | medium | [`use-pdf-viewer-page-rotation.ts:47`](../../src/components/pdf-viewer/use-pdf-viewer-page-rotation.ts) | a rebuilt `pages` array resets every rotation | ◐ FIXED |
| F3 | low | [`use-date-picker-state.ts:157`](../../src/components/date-picker/use-date-picker-state.ts) | a clear emits `onOpenChange(false)` on a closed picker | ◐ FIXED |
| F4 | low | [`use-date-picker-state.ts:285`](../../src/components/date-picker/use-date-picker-state.ts) | `aria-activedescendant` outlives the grid it names | ◐ FIXED |
| F5 | low | [`menu-item.tsx:96`](../../src/components/menu/menu-item.tsx) | a link item does not activate on Space | ◐ FIXED |
| F6 | low | [`use-menu-state.ts:151`](../../src/components/menu/use-menu-state.ts) | `data-active` survives the exit animation | ◐ FIXED |
| F7 | low | [`menu-trigger.tsx:149`](../../src/components/menu/menu-trigger.tsx) | consumer props spread after the structural wiring | ◐ FIXED |

### R1 — the `input` arm of DatePicker drops what the state hook resolved

`useDatePickerState` resolves `readOnly` and `invalid` for the whole component. The button-trigger arm forwards both. The `input` arm forwards `disabled` only, so each resolved flag dies at the branch. One decision fixes both rows: forward the resolved state to `DateInput` as the trigger arm does.

**R1.1 — `readOnly` never reaches the `DateInput`** · `date-picker.tsx:325`. The hook resolves `readOnly` at `use-date-picker-state.ts:63`, and its only reader is the `setOpen` guard at `:87`. The returned bundle never exposes it. `<DatePicker input readOnly value={d} onValueChange={spy} />` therefore renders an editable field with a live clear button, and a keystroke runs `commit` and rewrites the bound form field. The prop's own doccomment at `date-picker.tsx:161` says it blocks a value change. The input arm renders a bare `data-slot="control"` div, not a `Control` provider, so the `Control` half of the cascade is absent too. The one `readOnly` test covers the button trigger only.

**R1.2 — the resolved `invalid` flag never reaches the `DateInput`** · `date-picker.tsx:316`. The hook merges the `Control` severity with the bound field's errors at `use-date-picker-state.ts:267`. `DateInput` re-derives the `Control` half through `useControl()`, so only the form-field half is lost — and that half is the one a resolver sets. Submit a `Form` whose resolver rejects an empty `due`, and the typed field carries no `aria-invalid` and no `data-invalid`. The same tree marks the button trigger invalid. A screen-reader user in `input` mode is never told the control is in error.

### R2 — relative presets have no span identity

A relative preset resolves to a span, and the code treats the preset id and the span as interchangeable. They are not: two presets can resolve to one span, and one span can change which preset it matches as the clock moves. Three findings follow from that single conflation, and R2.3 reaches its trigger only through R2.2.

**R2.1 — multi-select dedupes preset ids, not spans** · `date-picker-relative-utilities.ts:267`. The multi branch rebuilds the value as `presets.filter(selected.has(id)).map(resolve)`. Dedupe runs on the id set, and the committed value is a bare span, so two presets that resolve to one span both emit it. The shipped presets collide: `this-month` and `this-year` resolve identically for every day of January, and `today` and `this-month` collide on the first of each month. The value becomes `[span, span]`. `relativeChips` labels both spans through the newest pick, so two chips render with one React key — a duplicate-key error, and an aliased second chip. `selectedPresetIds` collapses both to one id, so the twin row reports `aria-pressed="false"` although it is selected, and a second click re-adds the same pair: the row cannot toggle off. The function's TSDoc at `:239` promises a deduped rebuild. The shipped demo passes `relative={{ multiple: true }}` with the default presets, and the only collision test covers single-select.

**R2.2 — the reference clock sits in a ref that no memo depends on** · `use-date-picker-relative-state.ts:136`. `nowRef` takes `new Date()` during render, and `openPicker` re-stamps it. The two derivations that consume it, `chips` and `selectedIds`, list neither the ref nor any value that changes with it; `presets` resolves to a module constant for every shipped option, so no dep moves. A ref write cannot invalidate a memo, so the re-stamp never reaches the rendered label or the highlight. Hold a page across midnight with `Today` selected and reopen: the trigger still reads `Today` for a span that is now yesterday, and the `Today` row keeps `aria-pressed="true"` while `togglePreset`, which reads the ref directly, resolves at the fresh clock and commits a new span instead of a clear. `Calendar` guards the same hazard by deferring `today` to a mount effect. Hold `now` in state, seed it in an effect, and list it as a dep. The claim also named `customActive` as stale; it is computed inline, so it reads the fresh ref and is sound.

**R2.3 — the custom-range seed reads `value[0]`, not the unmatched span** · `use-date-picker-relative-state.ts:153`. `customActive` asks whether any committed span matches no preset, and `customSpan` then takes `value[0]` unconditionally. The committed value is never a mixed array, so a consumer-supplied mixed array is out of contract and is not a trigger. R2.2 supplies the real one: with `multiple: true`, pick two presets and hold the page across midnight. At the re-stamped clock `value[0]` matches a preset and `value[1]` matches nothing, so `Custom range` pre-fills Start and End from the preset span. Both endpoints are present, so the draft reads as a settled range that no user entered. Select the span that matched nothing.

### R3 — a static Menu with a trigger is an unsupported composition

`MenuTrigger`'s own TSDoc scopes it to a dropdown `Menu`, and `MenuContent` documents a static menu as a panel with no trigger to name it. Nothing enforces that, so `<Menu defaultOpen>` with a `MenuTrigger` typechecks and renders — and the repository's own accessibility corpus builds exactly that shape at `__tests__/a11y/cases/overlays.tsx:100`, with a comment that calls it a dropdown. Three findings come from that one seam. [`2026-07-13-PROPS-AUDIT.md`](2026-07-13-PROPS-AUDIT.md) already holds an open row for the seam itself, quoted here because that audit gets deleted when its findings close: "`placement` present = dropdown, absent = context, `defaultOpen` = inline — three modes; ContextMenu already exists as one explicit variant, a `StaticMenu` would complete the set". That row names the API shape; these three rows name the defects it produces, so they are new. One decision closes R3.2 and R3.3 and makes R3.1 the only real work: make the unsupported composition impossible, or support it.

**R3.1 — a static menu seats no tab stop** · `menu-content.tsx:73`. The static branch renders `PopoverPanel` with `autoFocus={false}`. `PopoverPanel` runs `useA11yRoving` without `manageTabIndex`, which defaults to false, so the tab-stop effect returns at its first line. The panel is `tabIndex={-1}` and every row hardcodes `tabIndex={-1}`. Nothing in the subtree is tabbable, and nothing seats focus, so the documented roving focus has no entry point and a pointer hover is the only way in. A keyboard user cannot reach or activate any row (WCAG 2.1.1). Pass `manageTabIndex` through for the static branch, or give the static panel `tabIndex={0}`.

**R3.2 — a static menu's trigger toggles state the panel ignores** · `use-menu-state.ts:158`. `useClick(context)` composes into `getReferenceProps` in every mode. `MenuContent` gates the static panel on `isStatic`, not on `open`. A press therefore flips `open` to false and fires `onOpenChange(false)` while the panel stays on screen, so the trigger reports `aria-expanded="false"` over a visible menu and drops `aria-controls`. Only dismissal is suppressed for static mode; the toggle is not. Disable the toggle: `useClick(context, { enabled: !isStatic })`.

**R3.3 — a static panel omits `id`, so `aria-controls` dangles** · `menu-content.tsx:64`. The floating branch stamps `id={menuId}`; the static branch does not. `MenuTrigger` emits `aria-controls={open ? menuId : undefined}` in both arms, and a static menu's `open` is true from mount, so the trigger always names an element that never enters the document. axe reports a missing `aria-controls` target as incomplete rather than as a violation, so the accessibility sweep gate does not catch it. Stamp `id={menuId}` on the static panel.

### Independent findings

**F1 — the footer Clear lands focus on the panel, not on a row** · `use-date-picker-relative-state.ts:212`. `handleClear` empties the value and keeps the popover open on purpose. An empty value makes `showFooterClear` false, so `DatePickerFooter` returns null at `date-picker-footer.tsx:41` and removes the focused button with no hand-off. `useFloatingUI` returns focus on an open-to-closed transition, which `handleClear` avoids, and `restoreFocus` defaults to false. What happens next was measured in the browser suite rather than reasoned: `FloatingFocusManager` re-seeds its own initial focus, which is the `role="dialog"` panel container, so focus is never lost. The cost is a worse landing, not a trap — the container takes no arrow key, and a screen reader re-announces the whole dialog from it. Only keyboard activation reaches the case, because the panel preventDefaults `mousedown`; the one relative consumer in `apps/places` passes `footer={{ clear: false }}`. Move focus inside `handleClear`, as the trigger's clear button does.

The claim as raised said focus lands on `document.body` against WCAG 2.4.3. That is what the jsdom double produces, because it re-seeds nothing, and it is what the verification accepted. A real browser puts focus on the container with the fix removed and on the first preset with it in place; `document.body` in neither. The severity follows the measurement down to low, and `browser/floating-ui/date-picker-footer-clear-focus.test.tsx` now holds the guarantee, because jsdom with the live engine gives a third answer again.

**F2 — a rebuilt `pages` array resets every rotation** · `use-pdf-viewer-page-rotation.ts:47`. The document identity is `pagesProp ?? src`, compared by reference against a ref, and a mismatch clears every stored rotation during render. `PdfViewer` hands the prop through with no memo and carries no `React.memo`, so `pages={docs.map(toPage)}` yields a fresh array for each parent render and each one reads as a document swap. Rotate a page, then trigger any unrelated parent render: every rotation is lost. The hook's contract says a page preserves its rotation, and `PdfViewerProps.pages` documents no stability requirement — while the sibling `highlights` prop advertises that its identity need not be stable, so the inline array is the shape the API invites. Key the reset on `src` or on the joined page ids.

**F3 — a clear emits `onOpenChange(false)` on a closed picker** · `use-date-picker-state.ts:157`. `handleClear` always calls `closeCalendar`, which calls `setOpen(false)` unconditionally, and `useControllable`'s setter fires the callback with no equality check. Clear from the trigger without opening the picker and the consumer receives a close transition that never happened. The range hook holds the same shape. Guard the close, or let `setOpen` skip a write that matches the current state.

**F4 — `aria-activedescendant` outlives the grid it names** · `use-date-picker-state.ts:285`. `inputAria` gates `aria-controls` on `open` and gates `aria-activedescendant` on `active?.zone === 'grid'` alone. `active` clears in `openCalendar` and `closeCalendar` only. A close by a click or a press outside self-heals through the outside-press dismiss, so that is not a trigger; a programmatic close of a controlled `open` prop — an effect, a timer, a route change, or arriving data — leaves the input naming an id that the unmounted grid no longer stamps, and a later programmatic reopen restores the stale highlight. Gate both attributes on `open`, and reset `active` when `open` goes false from any source.

**F5 — a link row does not activate on Space** · `menu-item.tsx:96`. The `href` branch composes `onClick` and `onPointerMove` and no `onKeyDown`; the button branch composes the key handler. An anchor activates on Enter natively, so Enter works. Space does not: `handleActivationKey` bails when the mode is not virtual, typeahead excludes the space key, and the navigation helper returns null for it, so nothing consumes the press and the page behind the open menu scrolls. This affects the context menu, a submenu, and a static menu; the dropdown path synthesises a click. No demo and no repository consumer puts an `href` row outside a dropdown, so this is an APG gap on one redundant key, not keyboard inoperability.

**F6 — `data-active` survives the exit animation** · `use-menu-state.ts:151`. `clearVirtualActive` forwards an empty item list, so the lookup that strips `data-active` matches nothing and only the owner's `aria-activedescendant` is removed. Its comment assumes the rows unmount, but `PresencePortal` holds the panel through a 150 ms opacity exit. Reopen inside that window and the same DOM nodes reconcile, so a row stays washed with no `aria-activedescendant` that names it, and the first arrow press skips past it. A reduced-motion user is unaffected, which makes the divergence intermittent. Clear from the live rows, or strip the attribute across the panel on open.

**F7 — consumer props spread after the structural wiring** · `menu-trigger.tsx:149`. `mergeProps` in the installed floating-ui build assigns every non-`on*` key straight through, so `{...getReferenceProps({ ...rest })}` re-emits each consumer prop — and that spread sits after `type="button"`, `aria-haspopup`, `aria-expanded`, `aria-controls`, and `data-slot`. A consumer prop therefore wins over the structural wiring, which inverts [`CONVENTIONS.md`](../../../../CONVENTIONS.md) §3.9. `MenuTriggerProps` accepts `type`, so `<MenuTrigger type="submit">` inside a form typechecks, and a press both opens the menu and submits the form. This component's own cloned-child branch applies the wiring after the spread, and `menu-item.tsx` is the convention's cited precedent. Move the five attributes below the spread.

## Ruled out

No claim fell, so this section holds no dead finding. It holds the two triggers a verifier refuted, because those name ground the next sweep does not need to walk again.

`R2.3` — the claim's trigger was a consumer-supplied array that mixes preset spans with custom spans. The documented mutual-exclusivity contract excludes that value, and the committed value is never mixed. The finding survives on a different trigger, which `R2.2` supplies.

`F4` — the claim's trigger was a `Done` button outside the picker. A click or a press anywhere outside routes through the outside-press dismiss, which clears `active` and erases the state the claim depends on. Only a non-pointer programmatic close reaches the defect.

### Severity changes

The red team set four severities against reach, and each change records why no shipped surface carries the defect today.

`R3.1` — high to medium. Static mode has no consumer in the monorepo and no demo, so nothing shipped is keyboard-unreachable.

`R3.2` — medium to low. The defect needs a `MenuTrigger` inside a static menu, which no demo builds and which the trigger's own TSDoc excludes.

`F1` — medium to low, at resolution and not by the red team. The claimed `document.body` landing is the double's behaviour; a browser re-seeds focus to the panel container, so the defect is a worse landing rather than a keyboard trap.

`F5` — medium to low. The gap costs one redundant key, and Enter still activates the item.

`F6` — medium to low. The divergence holds for 150 ms, and the next arrow press or pointer move heals it.

## Recommended resolution — `A01`

Five steps close the 15 findings. The order is deliberate: step 2 sequences internally because one fix changes what another one sees, and step 3 waits on an answer. Nothing here adds, removes, or renames a public export, so §12.2 leaves the surface indexes alone.

**S1 — forward the resolved state to `DateInput`.** Closes `R1.1` and `R1.2`. Done on the branch, and it waits on a pull request. `useDatePickerState` resolves `readOnly` at `use-date-picker-state.ts:63` and `invalid` at `:267`, and the returned bundle exposes neither. Add both to the bundle and spread them onto `<DateInput>` in the `input` arm, as the button arm already does. Low risk: the button arm is the precedent, and the two tests mirror the `readOnly` and `invalid` tests that already cover it. Do this first — it is the highest severity and the smallest change.

**S2 — give relative presets a span identity.** Closes `R2.2`, then `R2.1`, then `R2.3`, in that order. Done on the branch, and it waits on a pull request. Take `R2.2` first: move the reference clock out of the render-time ref into state, seed it in a mount effect as `Calendar` does at `calendar.tsx:166`, and list it as a dependency of `chips`, `selectedIds`, and nothing else — `customActive` already reads fresh and must stay inline. `R2.1` then dedupes the rebuilt spans so a collision commits one span. `R2.3` comes last, because its reachability turns on `R2.2`. It still reproduces after the fix, and the fix is what makes it reachable: once the instant re-stamps, the picker correctly sees a matched span ahead of the unmatched one. Carries one open question, below.

**S3 — make a static `Menu` trigger inert.** Closes `R3.1`, `R3.2`, and `R3.3`. Done on the branch, and it waits on a pull request. The review settled the seam: forbid the composition rather than bless it. TypeScript cannot forbid a child by an ancestor's props, so the enforceable form is behavioural. In static mode `MenuTrigger` stops toggling — `useClick(context, { enabled: !isStatic })` — and it emits no disclosure ARIA, so `aria-expanded` no longer contradicts a visible panel (`R3.2`) and `aria-controls` no longer names an element that never renders (`R3.3`). Neither fix needs an `id` on the static panel. Say the constraint in both doccomments, because the types cannot.

`R3.1` is independent of that decision and still needs fixing: a static panel with no tab stop is keyboard-unreachable whether or not a trigger sits beside it. Seat the tab stop without seating focus — `menu-content.tsx:71` carries a comment that `autoFocus={false}` is deliberate, so a fix that autofocuses the panel breaks documented intent.

**S4 — three independent defects with observable user cost.** Closes `F1`, `F2`, and `F3`. Done on the branch, and it waits on a pull request. `F1` moves focus inside `handleClear` before the footer can unmount, as the trigger's clear button already does. `F2` keys the rotation reset on the joined page sources; `PdfViewerPage.src` is required (`types.ts:9`), and `src` alone does not serve, because a consumer that passes `pages` may pass no `src`. `F3` guards the close so a clear on a shut picker emits no transition. Independent of each other, so one pull request or three.

**S5 — four low-severity rows.** Closes `F4`, `F5`, `F6`, and `F7`. Done on the branch, and it waits on a pull request. `F7` is a five-attribute move below the `getReferenceProps` spread and is the one to review closely, because §3.9 is the convention it restores and `menu-item.tsx` is the cited precedent. `F4`, `F5`, and `F6` are local. Take this step last, or drop it into whatever pull request touches those files next.

`F6` carries its test at `clearVirtualActive` rather than at the menu's close effect. The exported function takes a plain DOM, so the rows branch needs no engine, no portal, and no animation. The wiring is one call, and no suite can drive it: the exit window exists only under `motion/react`, which the jsdom projects mock as instant, and timing a 150 ms exit in the browser is the async lifecycle §10.3 bars.

One test-tree change rides along. The accessibility corpus builds a static menu at `__tests__/a11y/cases/overlays.tsx:100` under a comment that calls it a dropdown. Whatever S3 decides, that case is mislabelled today and its comment must match what it renders.

### Settled questions

The review answered three questions, and the answers are in the steps above.

**The static `Menu` seam — forbid the composition.** Static mode has no consumer in the monorepo and no demo, so no option carried migration cost. Forbidding is the smallest change and it matches what both components already document; supporting the shape would have meant rewriting two doccomments to bless it. The `StaticMenu` variant that [`2026-07-13-PROPS-AUDIT.md`](2026-07-13-PROPS-AUDIT.md) proposes stays that audit's business, and this decision does not block it.

**The first paint of a relative picker — accept one frame.** `R2.2`'s fix defers the reference clock to a mount effect, so the first render resolves presets against the seed value and the effect corrects it. `Calendar` sets that precedent for `today` at `calendar.tsx:166`, and following a precedent beats inventing a second pattern for one hazard.

**Resolution cadence — one segment at a time.** Each segment resolves before the next starts research. The plan carries the reasoning.

## Method correction

The first verification pass kept all 15 claims. This repository's audits kill a large share of theirs. The events audit killed 19 of 54, the August simplification audit 35 of 57, and the test-suite audit 8 of 26. A zero rate is therefore a fact about the verification, not a result, so a red team re-judged all 15 with a mandate to overturn.

It overturned none. On mechanism the sweep was disciplined. The red team opened every cited file and reproduced all 15 traces. It found no claim whose cited line says something other than what the claim says, so the zero rate is partly earned.

The verification was weak in four ways.

First, neither verifier attacked the trigger. Both attacked the mechanism, found it, and stopped. A claim whose mechanism is real but whose stated trigger cannot reach it survived twice — `F4` and `R2.3` are the two proofs.

Second, neither verifier weighed reach. Two greps settle who reaches each path, and they moved four of the fifteen severities. Both verifiers read the word reachability as "can this code path execute" in place of "does anything execute it".

Third, the batch did not cross-reference its own claims. `R3.1`, `R3.2`, and `R3.3` are one unsupported composition, and [`2026-07-13-PROPS-AUDIT.md`](2026-07-13-PROPS-AUDIT.md) already holds an open row for the seam. Judged one at a time each confirms; judged together they are one decision.

Fourth, the two verifiers anchored on the same text. They ran in parallel and neither read the other's output, so the anchor is the claim itself. Both read the sweep's own evidence field and walked the same citations in the same order. One of those citations was wrong: `F1` named `date-picker-footer.tsx:247` in an 86-line file, and the real bail is `:41`. A verifier corrected it from the claim rather than from the file, and neither verifier quoted the file. Both also cited `node_modules` line numbers, and neither checked `patches/`, where this tree patches `@floating-ui/react`. That patch touches an unrelated export, so `F1` survives by luck rather than by verification.

Resolution then found two more, and neither verifier could have reached either one.

Fifth: a probe can anchor to a test double as readily as a citation can. `R3.2` was refuted on a probe that showed a press on a static menu's trigger reporting nothing. That probe ran in the jsdom projects, which mock `@floating-ui/react`; the double suppresses the click-close for any surface a click did not open, and the live `useClick` does the opposite for that one case, because `stickIfOpen` tests an `openEvent` a `defaultOpen` surface never records. The row is real, the refutation was wrong, and the guard `S3` prescribes is restored. A claim about third-party behaviour is now provable only in `src/__tests__/browser/floating-ui`, where the engine is live, and the guarantee sits there.

`F1` then showed that "live" has three states, not two. The row claimed focus lands on `document.body`, which is the double's behaviour. A browser seats the panel container with the fix removed and the first preset with it in place, and `document.body` in neither — so the fix is right and the stated consequence was not. jsdom with the real engine gives a third answer again, because its focus timing is not the browser's. Focus, layout, and paint need the browser project; the real engine inside jsdom settles a synchronous call and nothing more.

Sixth, in a commit body rather than in a finding. `7e92eb0` attributes two unused declarations to the `packages/ui` Biome override. The two rules are `correctness/noUnusedImports` and `correctness/noUnusedVariables`, and both come from the base `recommended` preset; the override adds `complexity/noExcessiveCognitiveComplexity` alone, and it excludes the test file that held `footerClear`. Measured on the parent revision: the root run and the package script each print both diagnostics, as warnings, and each exits 0. The body's last clause is true by itself — the override does miss the package directory, because `overrides.includes` resolves against the scan root — and it names a gate gap that no job holds. The misattribution is the fault. A commit body is a claim like any other, and this one took no measurement.

Six changes apply from segment `A02`. The batched mechanism pass stays as it is, because it earns its cost and it is what holds the verification affordable.

1. Add the reach pass that [the plan](../plans/2026-09-11-BUG-SWEEP-PLAN.md) defines. Expect it to move severities rather than kill claims.

2. Deny one verifier the sweep's evidence field. Give it the file, the symptom, and the claimed wrong result, and require it to locate the mechanism itself. A shared wrong citation then cannot propagate, which fixes the fourth fault at its cause rather than at its symptoms.

3. Run any probe of a mocked dependency where that dependency is live. `src/__tests__/mocks` lists what the jsdom projects replace, and a probe inside one of them measures the replacement. This is the fifth fault, and a probe is the one instrument a reader trusts without re-deriving it. For focus, layout, or paint, live means the browser project: the real engine inside jsdom settles a synchronous call and nothing more.

4. Require a verifier to quote each line from the file it opened. The wrong citation in `F1` would not have survived one quote.

5. Require the batch to group its claims by root cause. Before it reports, it must search the open audits for a row that already records the seam.

6. Measure a claim about a tool in the invocation that runs it. A commit body, a pull-request body, and a step paragraph each carry claims, and none of them gets a verifier.

A verifier must also cite the code the build resolves, not the code the package name implies. Check `patches/`, the `overrides` block, and the `paths` and `resolve.alias` entries before citing a `node_modules` line. The narrower rule — check `patches/` — misses an alias, and this package already vendors one dependency for the same reason.

The cost of segment `A01` sets the budget for the rest. Two sweeps raised 15 claims. The verification spent 643,106 agent tokens across 4 agents, and the red team spent 346,960 across 2. The verification therefore cost about one million tokens for 15 claims, and it cost more than the sweep that found them.

## Findings — `A02`

The batch judged 34 claims, and 21 distinct defects survive. The row ids repeat the prefixes that `A01` uses, so each id below reads inside this section alone. `Status` tracks resolution only, and resolution has not started. Every row is researched and unresolved, so each one reads `◯ OPEN`. No row of this segment carries a pull request. The sections below carry the mechanism only, and [Recommended resolution — `A02`](#recommended-resolution--a02) carries the work.

Two units make up the segment: `A02-u1` (`1e3a282`, 14 files, calendar) and `A02-u2` (`8989cbd`, 57 files, data-display 1/2). One blind, batched `bug-verifier` pass judged both units over the union of two independent readings. A second, independent judge later re-judged the same stripped claims, and it agreed on survive-or-die for 34 of 34 claims. Its three differences sit on the rows that carry them. Seven defects were named twice, once by each reader, and the verifier confirmed each pair as one defect; each row below is the defect and not the claim.

Nine root causes produce 15 of the 21 findings. Each root cause resolves as one decision, so the groups come first.

| # | Severity | Location | Defect | Status |
|---|---|---|---|---|
| R1.1 | medium | [`use-calendar-focus.ts:163-171`](../../src/components/calendar/use-calendar-focus.ts) | the calendar's roving hooks never take `tabIndex` ownership | ◯ OPEN |
| R1.2 | medium | [`use-calendar-picker.tsx:169`](../../src/components/calendar/use-calendar-picker.tsx) | the picker's year grid marks the calendar's year, not the picker's | ◯ OPEN |
| R1.3 | medium-high | [`calendar.tsx:42-47`](../../src/components/calendar/calendar.tsx) | the rendered month is write-only, so a parent's focus write discards its navigation | ◯ OPEN |
| R1.4 | low | [`calendar-range.tsx:129-141`](../../src/components/calendar/calendar-range.tsx) | `CalendarRange` never re-anchors the view after a parent sets an endpoint | ◯ OPEN |
| R1.5 | low | [`calendar.tsx:176-180`](../../src/components/calendar/calendar.tsx) | a form-bound `Calendar` still seeds its month from `defaultValue` | ◯ OPEN |
| R1.6 | low | [`use-calendar-month.ts:57-63`](../../src/components/calendar/use-calendar-month.ts) | two `CalendarHandle` month calls in one handler coalesce | ◯ OPEN |
| F1 | medium | [`use-calendar-focus.ts:190-217`](../../src/components/calendar/use-calendar-focus.ts) | the day grid cannot cross a month boundary with an arrow | ◯ OPEN |
| R2.1 | medium | [`json-tree.tsx:76-79`](../../src/components/json-tree/json-tree.tsx) | the virtualized `JsonTree` keeps the unwindowed keyboard model | ◯ OPEN |
| R2.2 | medium | [`tree.tsx:59-63`](../../src/components/tree/tree.tsx) | `Tree` roving walks into a hidden held treeitem | ◯ OPEN |
| R2.3 | low | [`tree-utilities.ts:19`](../../src/components/tree/tree-utilities.ts) | `ensureFirstItemActive` cannot recover a tab stop stranded on a hidden item | ◯ OPEN |
| R2.4 | low | [`json-tree-virtualized.tsx:58-66`](../../src/components/json-tree/json-tree-virtualized.tsx) | the virtualized search seed never resets when the term clears | ◯ OPEN |
| R2.5 | low | [`json-tree-virtualized.tsx:49-53`](../../src/components/json-tree/json-tree-virtualized.tsx) | the virtualized expansion set is seeded once and never re-derived | ◯ OPEN |
| R2.6 | medium | [`kanban-card.tsx:73`](../../src/components/kanban/kanban-card.tsx) | the kanban card's key handler consumes a descendant's keys | ◯ OPEN |
| R2.7 | low | [`kanban-column-body.tsx:15`](../../src/components/kanban/kanban-column-body.tsx) | `KanbanColumnBody` reads a falsy child as "has children" | ◯ OPEN |
| R2.8 | medium | [`recipes/kata/kanban.ts:30`](../../src/recipes/kata/kanban.ts) | the kanban column's drop-target highlight contributes no class | ◯ OPEN |
| R2.9 | low | [`use-list-drag.ts:22-28`](../../src/components/list/use-list-drag.ts) | the read-only `List` key fallback collapses equal items | ◯ OPEN |
| R2.10 | low | [`list-item.tsx:150-151`](../../src/components/list/list-item.tsx) | `href` with `interactive={false}` puts two tab stops on a reorderable row | ◯ OPEN |
| R2.11 | low | [`tree-item-children.tsx:14-32`](../../src/components/tree/tree-item-children.tsx) | `stampTreePositions` counts wrappers, not items | ◯ OPEN |
| R2.12 | low | [`json-tree-node.tsx:78`](../../src/components/json-tree/json-tree-node.tsx) | `rootKey=""` desynchronizes node paths from the exported collector | ◯ OPEN |
| R2.13 | low | [`json-tree.tsx:67`](../../src/components/json-tree/json-tree.tsx) | uncontrolled `JsonTree` toggles survive a data swap | ◯ OPEN |
| R2.14 | low | [`stat-delta.tsx:11`](../../src/components/stat/stat-delta.tsx) | `StatDelta` documents a `flat` trend the recipe does not define | ◯ OPEN |

### G1 — `useCalendarFocus` omits `manageTabIndex` on both roving calls, so neither zone seats a stop

**R1.1 — the calendar's roving hooks never take `tabIndex` ownership** · `use-calendar-focus.ts:163-171`. Neither `useA11yRoving` call passes `manageTabIndex`, which defaults to false at `use-a11y-roving.ts:876`. The tab-stop effect therefore returns at its first line (`:899`), and arrow roving seats no stop (`:524`). Nothing else writes a `tabIndex`: `Button` renders a bare `<button>` (`button.tsx:172-183`), and no calendar file writes one. The `role="toolbar"` header (`calendar-header.tsx:54-57`) and the `role="listbox"` grid (`calendar-grid.tsx:68-72`) both present as single-stop widgets. A bare `<Calendar />` therefore costs about 33 Tab stops — three header buttons plus one for each enabled day. The hook's own TSDoc claims the pattern it does not implement: "Wires roving-tabindex keyboard navigation across a calendar's header, grid, and footer zones". `use-a11y-roving.ts:788-793` names the only two exemptions, and neither applies. `toolbar.tsx:41-42` is the house precedent, with `manageTabIndex: true` under the comment "Single Tab stop: Tab enters and leaves the group; arrows move between controls"; eight other call sites pass it. **Reach:** `docs/demos/components/calendar.tsx:25` and `:29`; through `DatePicker` at `apps/places/src/components/place-form-drawer/place-form-drawer.tsx:173`, `modules/query/query-builder/query-builder-rule-value.tsx:122`, `docs/demos/components/date-picker.tsx:19`, `filters.tsx:122`, `providers/glass.tsx:147`, and `modules/grid/editable.tsx:121`. **Fix:** pass `manageTabIndex: true` to both calls, and decide where the grid's tab stop rests.

### G2 — `yearCells.selected` compares against the calendar's year instead of the picker's own

**R1.2 — the picker's year grid marks the calendar's year, not the picker's** · `use-calendar-picker.tsx:169`. `yearCells` computes `selected: y === year` against the calendar's rendered `year` prop (`:65`), while the decade in view is anchored on `pickerYear` (`calendar-picker-reducer.ts:50`, `use-calendar-picker.tsx:90`). The sibling month cells guard correctly at `:158` (`selected: i === month && pickerYear === year`), which is what makes this a slip rather than a design. With the calendar on June 2025, one "Next year" press sets `pickerYear = 2026`. Open the years view: it renders 2019-2030 and marks **2025** `aria-selected="true"` (`calendar-picker-grid.tsx:102`) with `data-selected` (`:105`). `focusPickerGrid` then seats keyboard focus on that cell (`use-calendar-picker.tsx:108-110`). Page the decade further and no cell matches at all, so focus falls to the first cell instead. `calendar-picker-reducer.ts:20` states the contract broken: "`pickerYear` and `decadeYear` track independently so paging the decade grid never disturbs the chosen month-year". **Reach:** the picker is the header's shipped centre trigger (`calendar-header.tsx:69-79`), so every call site above reaches it. **Fix:** compare against `pickerYear`.

*Second judge:* it ruled the deeper form RESTATED rather than CONFIRMED, because that form's stated first click lands on a year the grid does not render. The shallow form it calls reachable is the same defect on the same line.

### G3 — `Calendar` owns `viewDate` with no parent-visible reader and no parent-settable view, so every lever fails in one direction

**R1.3 — the rendered month is write-only, so a parent's focus write discards its navigation** · `calendar.tsx:42-47`. `CalendarHandle` exposes `prevMonth`, `nextMonth`, `openPicker` and `footerKeyDown`, and no reader. `navigateTo` (`use-calendar-month.ts:65-67`) is wired only to the internal picker at `calendar.tsx:263` and never leaves the component. `CalendarProps` carries no view prop and no view callback. A parent's only base is therefore `use-date-picker-state.ts:116-119` — `clampDate(value ?? min ?? new Date(), min, max)`. Open a value-less `DatePicker`, click "Next month" with the pointer, then press any arrow. `active` is null after `openCalendar` (`use-date-picker-state.ts:142`). `use-date-picker-keyboard.ts:158-163` therefore writes that guessed base into `active`, and the documented re-anchor at `use-calendar-month.ts:73-79` drags the view back. The month/year picker case is worse, because `selectMonth` routes through the internal `navigateTo` and the parent never sees it. `date-picker-content.tsx:189` preventDefaults `mousedown`, so the chevron click leaves DOM focus on the dialog and the next arrow routes straight to the null-active branch. **Reach:** `apps/places/src/components/place-form-drawer/place-form-drawer.tsx:173` is the trigger's exact shape on a live app route; also `query-builder-rule-value.tsx:122`, `docs/demos/components/date-picker.tsx:19`, `providers/glass.tsx:147`, and `modules/grid/editable.tsx:121`. **Prior art:** [`2026-08-13-EVENTS-AUDIT.md`](2026-08-13-EVENTS-AUDIT.md)`:66` holds an open `onMonthChange` row for this component, and `:43` already decided the principle — "a component that drives a position emits it". That row records the read half. It does not record that the invisibility corrupts `DatePicker`'s own arrow base. **Fix:** emit the rendered month, and base `getInitialActiveDate` on it.

*Second judge:* it set this row to high, and called it "the only claim in the batch whose trigger a real application route executes".

**R1.4 — `CalendarRange` never re-anchors the view after a parent sets an endpoint** · `calendar-range.tsx:129-141`. The endpoints reach `Calendar` only through `defaultValue`, and `value={undefined}` is hard-coded at `:140`. `useControllable` reads `defaultValue` once (`use-controllable.ts:30`), and `useCalendarMonth` seeds from it once (`:27-31`), so the recomputed `useMemo` at `:129` is dead after mount. Neither re-anchor rule fires: `activeGridDate` (`use-calendar-month.ts:73-79`) is not driven by `CalendarRange`, and the `value` rule (`:83-90`) is gated on a `value` that stays undefined. Mount with null endpoints, then set `rangeStart` from a parent preset: the grid keeps the mounted month with the band off-screen. **Reach:** NONE. The only consumer is `date-picker-range.tsx:66-77`, which structurally avoids it. The surface unmounts between opens, and every in-session `rangeStart` write comes from a click on a day already in view. **Fix:** drive the view through whatever the open question settles.

**R1.5 — a form-bound `Calendar` still seeds its month from `defaultValue`** · `calendar.tsx:176-180`. `useFormValue` correctly neutralises `defaultValue` for a bound field (`use-form-value.ts:51,56`). The raw prop is passed to `useCalendarMonth` beside the resolved value, so `use-calendar-month.ts:28` seeds `value ?? defaultValue ?? new Date()` from the prop the binding discarded. `clockSeeded` (`:37`) is false, so the mount correction at `:39-51` is skipped. `<Form defaultValues={{ date: null }}><Calendar name="date" defaultValue={new Date(2020, 4, 10)} /></Form>` opens on May 2020 with no selected day in it. [`CONVENTIONS.md`](../../../../CONVENTIONS.md) §7.2: "A bound field ignores the control's `defaultValue`/`defaultChecked`". **Reach:** NONE — no site passes both `name` and `defaultValue`, and `place-form-drawer.tsx:173` passes no `defaultValue`. **Fix:** pass the resolved value, not the raw prop.

**R1.6 — two `CalendarHandle` month calls in one handler coalesce** · `use-calendar-month.ts:57-63`. Both steppers compute `firstOfMonth(year, month ± 1)` from the `year` and `month` captured in the render closure rather than from a functional updater. Two calls in one batch therefore resolve identically, and the second overwrites the first. The same file already uses the updater form for the mount-drift correction at `:46-50`. **Reach:** NONE — `use-date-picker-keyboard.ts:247-249` is a three-way branch, so exactly one call fires for each keypress, and no repository surface has a "jump two months" control. **Fix:** make both steppers functional updaters.

### G4 — grid arrow navigation delegates wholly to a model that wraps over a one-month item set

**F1 — the day grid cannot cross a month boundary with an arrow** · `use-calendar-focus.ts:190-217`. `handleGridKeyDown` bridges only the vertical zone edges and delegates the horizontal keys to `gridRoving`, whose `nextIndexGrid` wraps (`keyboard-navigation.ts:83-86`, `wrap` at `:46-48`) over an item set that is one month (`calendar.tsx:182`). ArrowRight on the last day focuses the first day of the same month. The view never moves, so `monthLabel` does not change and `useA11yAnnouncements` emits nothing. `calendar.tsx:237-239` names the path the code does not provide: "Month navigation (header chevrons, picker, **arrowing across a boundary**) re-renders the grid silently; this announces the new view to screen readers (WCAG 4.1.3)." No test covers a month edge. **Reach:** `docs/demos/components/calendar.tsx:25` and `:29` — a standalone DOM-focus calendar. Inside `DatePicker` the virtual model crosses correctly (`use-date-picker-keyboard.ts:182-187`), so the claim in its universal form is ruled out and the bare-`Calendar` trigger stands. **Fix:** step the view at the edge, or amend the contract.

### G5 — the virtualized `JsonTree` branch keeps state and a keyboard model derived once and never re-derived

**R2.1 — the virtualized `JsonTree` keeps the unwindowed keyboard model** · `json-tree.tsx:76-79`. The roving handler is built with a bare `itemSelector` and no `mode`, `itemSource` or `activeIndexRef`. The same handler is handed to the virtualized branch (`:95`, `json-tree-virtualized.tsx:99`). `use-a11y-roving.ts:416-418` therefore takes the DOM-query arm and sees only mounted rows, and `keyboard-navigation.ts:36-38,65` wraps inside that window. `End` lands on the last mounted row, and the wrap at the list end returns to the first rendered row. The hook's own TSDoc names the unused remedy at `:826-832`. **Reach:** NONE — no consumer passes `virtualize` to `JsonTree`. Every call site omits it, and the `virtualize` hits in `apps/places` are `Grid`. **§10.3:** the drivable seam is `useA11yRoving` over a hand-built row list. The end-to-end guarantee has no synchronous seam, because §10.3 bars a test that drives the virtualizer. **Fix:** build a `VirtualItemSource` from the flat nodes, and pass it with an `activeIndexRef`.

**R2.4 — the virtualized search seed never resets when the term clears** · `json-tree-virtualized.tsx:58-66` · RESTATED. `if (!searchValue || seededSearchRef.current === searchValue) return` takes the empty-term arm **before** `:63` writes the ref. The ref therefore keeps the old term forever, and a re-apply of that term never re-seeds. The claim's stated trigger does not reach the wrong result. A collapse while the term is active is deliberate (`:55-57`: "Seeds matching branch paths once per search term; a seeded branch stays collapsible afterwards"), and a bare clear-and-retype leaves the seeded paths open, because `expand` only unions (`use-json-tree-expansion.ts:45`). The trigger that does reach it: search a term, clear it, collapse a seeded branch while no term is active, then re-apply the same term — the matches stay hidden. **Reach:** NONE. **Fix:** reset the ref on the empty arm, and key it on the term and the data together.

**R2.5 — the virtualized expansion set is seeded once and never re-derived** · `json-tree-virtualized.tsx:49-53`. `initial: () => collectPaths(...)` reaches `useControllable` as `defaultValue`, which `useState` evaluates once (`use-controllable.ts:30`, documented at `:11`). A later `data` or `defaultExpandDepth` re-derives nothing, so a virtualized tree does not open to `defaultExpandDepth` after a data swap. The recursive renderer disagrees for identical props, because `json-tree-node.tsx:55` recomputes `depth < defaultExpandDepth` on every render. **Reach:** NONE. **§10.3:** the seed is observable through `onExpandedChange` with no virtualizer, and `__tests__/boundary/json-tree.test.tsx:285-286` already does this and says why. **Fix:** re-seed on an input change while uncontrolled.

### G6 — every `[role="treeitem"]` query in `tree` counts rows the user cannot reach

**R2.2 — `Tree` roving walks into a hidden held treeitem** · `tree.tsx:59-63`. The roving call passes `ITEM_SELECTOR` (`tree-constants.ts:1`), and `queryItems` (`use-a11y-roving.ts:61-65`) applies no visibility filter — unlike `rowControls` at `:53`, which filters with `checkVisibility()`. Under `mount="always"` a closed branch stays in the DOM at `display: none` (`mount.ts:114`, `hold.tsx:32`, and `hold.tsx:21-22`: "A hidden Activity keeps its subtree in the DOM at `display: none` with state intact"). `moveTo` then calls `next.focus()` (`:526`) after `preventDefault()` has run (`:732`), so the arrow is consumed and focus does not move. The next visible sibling is unreachable while the branch stays closed. **Reach:** NONE — every shipped tree takes the default `mount="active"`, which unmounts a closed branch. **Environment:** jsdom's `focus()` ignores `display: none`, so the stated consequence is provable only in the browser project. **Fix:** filter the item query by visibility, or scope the selector.

**R2.3 — `ensureFirstItemActive` cannot recover a tab stop stranded on a hidden item** · `tree-utilities.ts:19`. The guard `items.some((item) => item.tabIndex === 0)` counts hidden held rows, because `:15` queries `ITEM_SELECTOR` unfiltered. A click seats the sole `tabIndex=0` on a nested row (`tree.tsx:132`, `tree-utilities.ts:7`). An external collapse under a held policy then leaves that stop inside a `display: none` subtree, and the tree has no keyboard entry point. Nothing recovers it. The observer watches `{ childList: true, subtree: true }` (`tree.tsx:116`), and a held collapse changes no child list; `mount.ts:86-88` states the wrapper is constant by design. The guard would also return early even if it ran. The contract is stated at `tree-utilities.ts:11-13`: "Keep the first treeitem tabbable as the rendered set changes (open/close, search, expand-all)". **Reach:** NONE — the trigger needs a held mount policy, externally controlled branches, a prior click and an external collapse, and no root builds even the first. **Fix:** make the guard require a focusable stop, and observe attribute mutations.

### G7 — the kanban card's key handler is not scoped to the card

**R2.6 — the kanban card's key handler consumes a descendant's keys** · `kanban-card.tsx:73`. `onKeyDown={interactive ? (event) => onCardKeyDown(cardId, event) : undefined}` carries no target guard. `use-kanban-keyboard.ts:322-349` filters only modifier keys before it routes Space into `handleCardSpaceToggle`, which calls `event.preventDefault()` unconditionally (`:98`) and announces "Picked up …" (`:106-111`). An arrow moves focus to an adjacent card (`:123`, `:238`). A control inside a card is therefore unusable from the keyboard. The sibling states and pins the opposite rule: `list-item.tsx:169-171` — "Keys bubbling from focusable descendants (buttons, inputs) belong to them, not the reorder gestures" — with `list.test.tsx:520`. **Reach:** NONE — every shipped card holds inert spans. **Prior art:** [`2026-08-04-SIMPLIFICATION-AUDIT.md`](2026-08-04-SIMPLIFICATION-AUDIT.md)`:239-250` proposes a merge of the two reorder hooks and enumerates their divergences; this one is not among them, so the row extends that one rather than duplicates it. **Fix:** add the `event.target !== event.currentTarget` guard at one of the two ends.

### G8 — `KanbanColumnBody` tests emptiness with a null check instead of the React-children count

**R2.7 — `KanbanColumnBody` reads a falsy child as "has children"** · `kanban-column-body.tsx:15`. `Array.isArray(children) ? children.length > 0 : children != null` passes for `false`, so the conditional-render idiom suppresses the `empty` placeholder and the column body renders blank. An array of `false` entries also reads as non-empty. The house answer is stated in this unit's sibling, `tree-item.tsx:65-69`: "`Children.toArray` drops `null`/`undefined`/`false` and empty arrays … A bare `!= null` check would announce `children={[]}` as a collapsed parent." **Reach:** NONE — every call site passes a bare `.map()` and none passes `empty`. **Note:** the `{cards.length && …}` form gives `children === 0`, which React renders as a literal `0` rather than as nothing; the placeholder is suppressed either way. **Fix:** count with `Children.toArray`.

### G9 — `useListDrag`'s index fallback keys a `Map` by the item

**R2.9 — the read-only `List` key fallback collapses equal items** · `use-list-drag.ts:22-28`. The fallback builds `new Map(items.map((item, index) => [item, index]))` and returns `String(indexByItem.get(item) ?? -1)`, so equal primitives resolve to one key — the *last* index, not the first. `list.tsx:55` documents "falls back to item index", and `use-list-drag.ts:16-17` repeats it. `['a','a','b']` gives both `'a'` rows key `"1"`, and index `0` never appears. Two `<li>`s then carry the same `data-item-id` (`list-item.tsx:189`), and `querySlot` resolves both to the first. **Reach:** NONE — the one call site with no `getKey` (`docs/demos/components/list.tsx:45`) passes distinct object literals. **Fix:** key by render position.

### Independent findings — `A02`

**R2.8 — the kanban column's drop-target highlight contributes no class** · `recipes/kata/kanban.ts:30`. `over: ''` composes at `kanban-column.tsx:72` as `cn(k.column.base, over && k.column.over, className)`, so the branch adds nothing and only `data-over` (`:67`) changes. No rule anywhere keys on that attribute: `data-over` appears nowhere in `packages/ui/src` or either app outside this line. `kanban-column.tsx:26-27` promises the behaviour: "Drop target and sortable context for one board column … **Highlights while a card hovers over it**". **Reach: YES — the only row in this segment that a shipped surface reaches.** `docs/demos/components/kanban.tsx:63-86` builds an interactive board, so the branch executes on every drag. **Fix:** fill the recipe slot, or drop the branch and say the highlight is the consumer's.

**R2.10 — `href` with `interactive={false}` puts two tab stops on a reorderable row** · `list-item.tsx:150-151`. The stop split keys off `interactive` (`:127`, `interactiveProp ?? activates`), not off `activates` (`:122`). An explicit `false` therefore makes `stopOnRow` true, and the `<li>` takes `tabIndex ?? 0` (`:165`, `:187`). The content area still renders a natively focusable `<a href>` (`:207-214`). The component states the invariant it breaks at `:79-83`: "A reorderable row has exactly ONE Tab stop … Wiring both put two indistinguishable stops on every row". The prop's own doc invites the configuration (`:28-30`). **Reach:** NONE for the stated trigger. The second judge records a live neighbour with the same root cause: `modules/grid/grid-column-manager.tsx:300-306` renders a reorderable row that holds a focusable `Checkbox` with neither `href` nor `onClick`, so that row has two stops today. **Environment:** tab order needs the browser project. **Fix:** derive the split from `activates`.

**R2.11 — `stampTreePositions` counts wrappers, not items** · `tree-item-children.tsx:14-32`. `Children.toArray` cannot see through a component boundary, so `setsize` counts wrappers and one `TreePositionContext` value cascades to every item inside one. A tree whose items come from a component announces `aria-posinset="1" aria-setsize="1"` on every row. `context.ts:23-27` anticipates a different divergence (collapsed branches) and not this one. `TreeProps.children` is a bare `ReactNode` that states no direct-child requirement. **Reach:** NONE — every shipped tree writes literal children. **Fix:** register position at runtime, or document and enforce the constraint.

**R2.12 — `rootKey=""` desynchronizes node paths from the exported collector** · `json-tree-node.tsx:78`. `path ? joinPath(...) : encodePathSegment(...)` branches on truthiness, not on "is root". With `rootKey=""` the root path is `''`, which is falsy. Every child therefore re-enters the root arm and mints `'tags'` where `collectJsonTreePaths` mints `'.tags'` (`json-tree-utilities.tsx:168,173`, `:56`). Controlled expansion then never matches (`json-tree-node.tsx:106`), and `onExpandedChange` emits paths the caller's own collector cannot reproduce. The collector is public (`index.ts:4`), so a consumer holds both halves. **Reach:** NONE — no consumer passes `rootKey`. **Fix:** branch on an explicit root marker.

**R2.13 — uncontrolled `JsonTree` toggles survive a data swap** · `json-tree.tsx:67`. `useRef(new Map())` is created once for each mount, keyed by path, never cleared and never pruned. `resolveNodeOpen` gives it precedence over both the search auto-open and the depth default (`json-tree-node.tsx:51`). A new document whose paths collide with the old one inherits the old document's toggles. The same file resets its other path-keyed derivation on `data` identity and says why (`json-tree.tsx:71-74`), so a scope by data is the file's own standard, applied everywhere but here. `context.ts:16-27` justifies the ref by ancestor collapse only. **Reach:** the second judge found the swap live at `docs/demos/modules/grid/index.tsx:527`, `:551`, `:574` and `docs/demos/components/form.tsx:83`, which re-render one instance with new `data`. The wrong state additionally needs a prior toggle on a same-named branch. **Fix:** clear the map on a data change while uncontrolled.

**R2.14 — `StatDelta` documents a `flat` trend the recipe does not define** · `stat-delta.tsx:11`. The doccomment says "tinted by `trend` (up/down/**flat**)", while the recipe axis is `{ up, down, neutral }` with `defaults: { trend: 'neutral' }` (`recipes/kata/stat.ts:30-35`). `StatDeltaVariants` derives the prop type from that axis (`:83`). `trend="flat"` is a type error, and the demo works around it: it writes `neutral` for the flat case (`docs/demos/components/stat.tsx:41`). §12.1 makes the doccomment part of the public surface. **Reach:** NONE for `flat` — the compiler prevents it. **Fix:** align the two names.

## Ruled out — `A02`

Six claims fell. Each one names ground the next sweep does not need to walk again.

**`calendar-day-cell.tsx` — an out-of-range option carries no `aria-disabled`.** Deliberate and documented: `use-calendar-focus.ts:8-13` — "Out-of-range cells render as `<button disabled>`, which can't take focus, so every query scopes to enabled buttons and roving skips disabled cells; `.focus()` on a disabled element is a no-op that would freeze the active index at the edge of a disabled range (WCAG 2.1.1)" — and pinned by `calendar.test.tsx:118`. The native attribute is the element-level mapping, so the claimed harm does not follow.

**`calendar.tsx` — `<Density scale>` collapses a split cascade.** The collapse is real; the consequence is not. `density.tsx:36-39` documents `scale` as the diagonal preset, and the cited `useControlSize` contract names its four owners at `:119-120` and excludes `Calendar`. No descendant of the calendar reads the `space` axis, and no consumer root wraps a calendar in a split `<Density>`. `date-picker-content.tsx:191` uses the same shape deliberately.

**`calendar.tsx` — a malformed locale tag throws.** The prop states its precondition (`calendar.tsx:96-99`, "BCP 47 locale tag"). The unvalidated hand-off is library-wide rather than a calendar defect (`resolve-locale.ts:20-21`, and the same shape in `date-picker-utilities.ts` and `currency-input.tsx`), and every tag in every consumer root is well formed.

**`calendar-range.tsx` — `hoverDate` paints a committed endpoint with no `rangeStart`.** The shipped parent gates it: `use-date-picker-range-state.ts:291` — `hoverDate: rangeStart !== null ? hoverDate : null`. The prop doc presupposes a started band, and every test that supplies `hoverDate` also supplies `rangeStart`. No repository consumer can construct the trigger.

**`json-tree-virtualized.tsx` — the window's spacers collapse to zero height.** The spacer maths is correct: `use-virtual-window.ts:87,91` give `topSpacer = virtualItems[0].start` and `bottomSpacer = totalSize - lastItem.end`, and both render as inline heights. The only all-zero state is an empty window, which the hook documents as the unmeasured commit (`:20-24`) and in which no rows render either, which contradicts the claim's own symptom. *Both judges flagged an adjacent question and ruled neither way on it: the scroll container is `inline-flex flex-col` (`recipes/kata/json-tree.ts:41`) and a contentless flex item is shrinkable. That is a different mechanism, the claim does not name it, and it needs a browser measurement to settle.*

**`pivot-table-pivot.ts` — `count` undercounts a group whose values are non-numeric.** Documented three times: `pivot-table-pivot.ts:22-25` — "skipping any cell that isn't a finite number or a numeric string (`null`, `''`, and other non-numeric values are dropped, not counted as `0`)" — `use-pivot-table.ts:63`, and `pivot-table-pivot.ts:56` — "`count` returns the length". *Both judges flagged an adjacent doc defect that no claim raised and ruled neither way on it: `pivot-table.tsx:39` documents `emptyCell` as "Rendered when no source rows match a (row × column) group", and rows do match a group whose values are all non-numeric.*

Two triggers were refuted inside rows that survive. `F1`'s "cannot be crossed with the keyboard at all" is false for the `DatePicker` composition, which crosses through its virtual model, and the row survives on the bare-`Calendar` trigger. `R2.4`'s two stated trigger arms each fail alone, and the row survives on the conjunction stated in its entry.

## Surfaced, not judged — `A02`

- `use-calendar-focus.ts:80-84` — `isTopRow` and `isBottomRow` compute the top row over the flat button list while `calendar-grid.tsx:95` offsets the 1st into its locale column, so the index row and the visual row differ in any month that does not start on the locale's first weekday.
- `use-a11y-roving.ts:61-65` — `queryItems` applies no visibility filter while `rowControls` at `:53` does; eighteen roving call sites share the unfiltered query. A cross-component seam for segment `C01`.
- `use-calendar-picker.tsx:102-112` — `focusPickerGrid` schedules a frame it never cancels.
- `json-tree-virtualized.tsx:85-90` — the tree's single Tab stop is picked out of the current window, so a scroll moves it, and a window that holds only `branch-close` rows leaves the tree with no stop at all.
- `use-kanban-keyboard.ts:139-150` — `Escape` and `Enter` share one body, so `Escape` drops the card at its new position and does not cancel the grab, against the APG pattern the hook cites.
- `kanban-card.tsx:75` — `aria-label` is gated on `interactive`, so a read-only board drops a consumer's accessible-name override.
- `context.ts:72` — `kanbanColumnTitleId` derives a document id from `columnId` alone, so two boards with a shared column id emit duplicate ids.
- `pivot-table-pivot.ts:18` — `resolveAxis` coerces with `String(row[key])`, so a nullish dimension renders a literal `"undefined"` header and merges with the string `"undefined"`.
- `list.tsx:173-181` — the `DragOverlay` clone renders an `<li>` outside any list with a live `tabIndex=0`.
- `tree-item.tsx:69` — `Children.toArray` counts an element that renders nothing, so a wrapper that returns `null` makes a leaf announce as a collapsed parent, against the comment at `:65-68`.

These lines came from later sweeps of the same scope and carry no verdict. They are not findings, and no row above depends on one.

## Recommended resolution — `A02`

Seven steps close the 21 findings. Each step states the order the verifier gave it, the files it touches, the question that gates it, and its test seam. The gate questions sit under [Open questions](#open-questions--a02) below, and none of them has an answer yet.

**S1 — scope the kanban card's key handler to the card.** Closes `R2.6`. Add the `event.target !== event.currentTarget` guard that `list-item.tsx:171` already carries, at one of the two ends, and mirror `list.test.tsx:520` for kanban. Files: `kanban-card.tsx` **or** `use-kanban-keyboard.ts`, plus `__tests__/components/kanban.test.tsx`. Take it first: it carries the highest cost of the group set, it has a sibling to copy, and it depends on nothing. Gate: Q1. Seam: synchronous.

**S2 — paint the column's drop-target highlight, or drop the promise.** Closes `R2.8`. Files: `recipes/kata/kanban.ts`, `kanban-column.tsx`, `__tests__/components/kanban.test.tsx`. Take it second: `R2.8` is the only row in the segment with reach. Gate: Q2. Seam: the composed class is synchronous; the paint needs the browser.

**S3 — seat the roving tab stop on both calendar zones.** Closes `R1.1`. Pass `manageTabIndex: true` to both calls, and decide where the grid's tab stop rests. Files: `use-calendar-focus.ts`, `__tests__/components/calendar.test.tsx`, plus a browser case for Tab order. Gate: Q3. Seam: the `tabIndex` invariant is synchronous; the Tab sequence is browser-only.

**S4 — compare the year grid against `pickerYear`.** Closes `R1.2`. Files: `use-calendar-picker.tsx`, and `calendar-picker-grid.tsx` only if Q4 moves `current` too. Independent of S3. Gate: Q4. Seam: `useCalendarPicker` returns the cells synchronously; the focus half needs the browser.

**S5 — give `Calendar` a parent-visible view.** Closes `R1.3`, then `R1.4`, `R1.5` and `R1.6`. Emit the rendered month, and base `getInitialActiveDate` on it. Feed `useCalendarMonth` the resolved value rather than the raw prop. Let `CalendarRange` drive the view, and make the steppers functional updaters. Files: `calendar.tsx`, `use-calendar-month.ts`, `calendar-range.tsx`, `date-picker/use-date-picker-state.ts`, `date-picker/use-date-picker-range-state.ts`, `docs/COMPONENTS.md` (§12.2 — this touches a public export), and [`2026-08-13-EVENTS-AUDIT.md`](2026-08-13-EVENTS-AUDIT.md)`:66` resolved in place against the pull request (§12.4). Gate: Q5. Seam: both hooks are `renderHook`-drivable; the end-to-end flow belongs in the browser project.

**S6 — decide where the roving item set learns what the user can reach, then fix both trees.** Closes `R2.2`, `R2.3`, `R2.1`, `R2.4` and `R2.5`. Files: `tree.tsx`, `tree-utilities.ts`, `json-tree.tsx`, `json-tree-virtualized.tsx`, and `hooks/a11y/use-a11y-roving.ts` only if Q6 answers "in the hook". Take it last of the behavioural steps. It has the widest diff. It is also the only step that can reach the hook's other roving call sites, and the only one gated on an unanswered question. Gate: Q6. Seam: partial — the tree half is synchronous; the windowed end-to-end guarantee has none, and §10.3 bars it.

**S7 — the local rows.** Closes `R2.7`, `R2.9`, `R2.10`, `R2.11`, `R2.12`, `R2.13`, `R2.14` and `F1`. Each one is local and independent, so take them in whatever pull request next touches those files. `F1` and `R2.10` carry gate questions (Q7, Q8); the rest do not.

### Open questions — `A02`

Eight questions gate the steps above, and research is not done until the reader answers them.

**Q1 — does the kanban target guard live on the card element or inside the reorder hook?** Axes: symmetry with `ListItem`, which guards in the component, against the open proposal to merge the two reorder machines ([`2026-08-04-SIMPLIFICATION-AUDIT.md`](2026-08-04-SIMPLIFICATION-AUDIT.md)`:239-250`, still open). It decides which of two files S1 touches, and whether S1 can land before that merge.

**Q2 — is a drop-target highlight the library's to paint, or the consumer's?** Axes: `kanban-column.tsx:26` promises one; `data-over` already exposes the state; `k.card.dragging` is also an empty slot, so a house pattern of reserved-but-unpainted slots can exist. One outcome fills the recipe; the other deletes the branch and rewrites the doccomment.

**Q3 — where does the day grid's tab stop rest?** Axes: the selected day, today, the first enabled day, or the last roved cell. A selected-day rule needs a fallback for a month with no selection. It changes only S3's options object.

**Q4 — in the years view, does `current` still mean today's year?** Axes: keep `current` on today while `selected` moves to `pickerYear`, or re-point both. It decides whether S4 touches `calendar-picker-grid.tsx` and its TSDoc.

**Q5 — is the calendar's view read-only to a parent, or writable?** Axes: emit-only (`onMonthChange`, the shape the events audit already proposes, precedent `PdfViewer.onPageChange`); or a controlled and uncontrolled `viewDate`/`defaultViewDate` pair under §7.3. Emit-only closes `R1.3`, and it closes `R1.4` only if `CalendarRange` becomes the parent that controls the view. A writable view closes all four G3 rows and adds a public prop pair. It sets S5's file set.

**Q6 — does the visibility rule belong in `useA11yRoving` or in each caller?** Axes: `queryItems` is the shared query and has no filter, while `rowControls` already pays `checkVisibility()`. A filter in the hook fixes G6 for all eighteen roving call sites at once, and it risks every one of them. Independently: does `JsonTree` adopt `itemSource`, or does the virtualized branch no longer advertise roving? Together they set S6's blast radius.

**Q7 — does a horizontal arrow at a month edge step the view, or stop?** Axes: APG day-grid conformance against the wrap contract the shared hook documents, and the cost of a calendar-specific branch in a hook with eighteen call sites. The step option needs Q5's answer first. It decides whether `F1`'s fix is behaviour or documentation.

**Q8 — for a row that is a link "in name only", which element holds the single stop?** Axes: the reorder keys must ride a focusable element, and an `<a href>` is focusable whatever `interactive` says. One outcome is a one-line predicate change; the other changes what `interactive={false}` means for every link row.

---

**See also:** [`CONVENTIONS.md` §12](../../../../CONVENTIONS.md) · [`REFERENCE.md`](../../REFERENCE.md) · [`2026-07-13-PROPS-AUDIT.md`](2026-07-13-PROPS-AUDIT.md) · [`../README.md`](../README.md).
